# Mobile build — replace YOUR_PC_IP with your computer's local IP address
# Find it with: ipconfig (Windows) → look for IPv4 Address under your WiFi adapter
# Example: 192.168.1.42
VITE_API_URL=http://YOUR_PC_IP:5000/api
VITE_SOCKET_URL=http://YOUR_PC_IP:5000
